on:
push:
branches:["main"]
pull request:
branches:["main"]

jobs:
build:
runs-on:ubantu-latest

steps:
-name:checkoutrepository
uses:actions 1 checkout@v4
-name:display message
run:echo"github actions workflow is running successfully!"
